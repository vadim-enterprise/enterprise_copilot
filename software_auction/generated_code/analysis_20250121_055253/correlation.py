# Find the correlation between different characteristics of existing customers and the likelihood of them becoming a customer
import pandas as pd\n\n# Assume `data` is your DataFrame\ncorr_matrix = data.corr()\n\n# Print the correlation matrix\nprint(corr_matrix)
